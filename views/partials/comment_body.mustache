<div class="commentContainer">
    <div class="commentHead">
        <div class="username{{#band_member}} band{{/band_member}}" >{{username}}</div>
        <div class="commentDate">{{comment_date}}</div>
    </div>
    <div class="commentBody">{{{comment}}}</div>
    <form id="replyToComment{{comment_id}}" class="commentReply" hx-post="/functions/interface/blog/reply_form.php" hx-target="#commentReply_{{comment_id}}" hx-swap="outerHTML">
        <input type="hidden" name="replyTo_username" value="{{username}}" />
        <input type="hidden" name="replyTo_body" value="{{comment}}" />
        <input type="hidden" name="comment_id" value="{{comment_id}}" />
        <input type="hidden" name="article_id" value="{{article_id}}" />
        <input type="hidden" name="tab_id" value="{{tab_id}}" />
        <input type="submit" value="Reply" />
    </form>
    <div class="commentReply" id="commentReply_{{comment_id}}" onclick="resizeHTMX(event)"></div>
</div>
{{#replies}}
    <div class="replyContainer">
        {{>comment_body}}
    </div>
{{/replies}}